def save_turnover(date, turnover):

    row = pd.DataFrame([{
        "date": date,
        "turnover": turnover
    }])

    file = "logs/turnover_log.csv"

    if os.path.exists(file):
        df = pd.read_csv(file)
        df = pd.concat([df, row])
    else:
        df = row

    df.to_csv(file, index=False)
