def save_ic(date, ic, rank_ic):

    row = pd.DataFrame([{
        "date": date,
        "ic": ic,
        "rank_ic": rank_ic
    }])

    file = "logs/ic_log.csv"

    if os.path.exists(file):
        df = pd.read_csv(file)
        df = pd.concat([df, row])
    else:
        df = row

    df.to_csv(file, index=False)
