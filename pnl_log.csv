import os
import pandas as pd

def save_pnl(date, net_return):

    row = pd.DataFrame([{
        "date": date,
        "net_return": net_return
    }])

    file = "logs/pnl_log.csv"

    if os.path.exists(file):
        df = pd.read_csv(file)
        df = pd.concat([df, row])
    else:
        df = row

    df.to_csv(file, index=False)
